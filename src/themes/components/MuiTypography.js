const headingFontWeight = `400`;

const MuiTypography = {
  styleOverrides: {
    h1: {
      fontSize: `22px`,
      fontWeight: headingFontWeight,
    },
    h2: {
      fontSize: `20px`,
      fontWeight: headingFontWeight,
    },
    h3: {
      fontSize: `18px`,
      fontWeight: headingFontWeight,
    },
    h4: {
      fontSize: `16px`,
      fontWeight: headingFontWeight,
    },
    h5: {
      fontSize: `14px`,
      fontWeight: headingFontWeight,
    },
  },
};

export default MuiTypography;
